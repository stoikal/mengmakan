import '../src/scripts/components/like-button';


describe('Add restaurant to favorites', () => {
  beforeEach(() => {
    const likeButton = document.createElement('button');
    

  });

  it('should show like button on restaurant that has not been liked') {

  }
});
